# 📤 Как загрузить код в GitHub

## Где выполнять команду `git push`?

Команду нужно выполнить **в терминале**, в папке вашего проекта.

---

## 📍 Шаг 1: Откройте терминал

### В Cursor (рекомендуется):

1. Откройте Cursor
2. Нажмите `Ctrl + ~` (или `Cmd + ~` на Mac) - откроется терминал внизу
3. Или меню: **Terminal → New Terminal**

### Или используйте системный терминал:

- **Mac**: Terminal.app (находится в Applications → Utilities)
- **Windows**: PowerShell или Command Prompt
- **Linux**: Terminal

---

## 📍 Шаг 2: Перейдите в папку проекта

В терминале выполните:

```bash
cd /Users/yury/Development/Personal/Books/Translate
```

Или если вы уже в Cursor, терминал автоматически откроется в правильной папке.

---

## 📍 Шаг 3: Проверьте статус

Убедитесь, что вы в правильной папке:

```bash
pwd
```

Должно показать:
```
/Users/yury/Development/Personal/Books/Translate
```

Проверьте статус git:

```bash
git status
```

Должно показать что-то вроде:
```
On branch main
Your branch is ahead of 'origin/main' by 1 commit.
  (use "git push" to publish your local commits)
```

---

## 📍 Шаг 4: Создайте репозиторий на GitHub (если еще не создали)

1. Откройте в браузере: https://github.com/new
2. Заполните:
   - **Repository name**: `book-translator`
   - **Description**: "AI-powered Russian to English book translator"
   - **Private** или **Public** (выберите нужное)
   - **НЕ добавляйте** README, .gitignore, license
3. Нажмите **"Create repository"**

---

## 📍 Шаг 5: Загрузите код

После создания репозитория на GitHub выполните в терминале:

```bash
git push -u origin main
```

**Что делает эта команда:**
- `git push` - загружает код на GitHub
- `-u origin main` - устанавливает связь между локальной веткой `main` и удаленной `origin/main`
- После первого раза можно будет просто писать `git push`

---

## ✅ Проверка успешной загрузки

После выполнения команды вы увидите что-то вроде:

```
Enumerating objects: 35, done.
Counting objects: 100% (35/35), done.
Delta compression using up to 8 threads
Compressing objects: 100% (35/35), done.
Writing objects: 100% (35/35), 150.23 KiB | 2.50 MiB/s, done.
Total 35 (delta 5), reused 0 (delta 0), pack-reused 0
remote: Resolving deltas: 100% (5/5), done.
To https://github.com/yugo-private/book-translator.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

---

## 🔐 Если запросит авторизацию

GitHub может запросить логин и пароль. Используйте:

**Вариант 1: Personal Access Token (рекомендуется)**
1. Создайте токен: https://github.com/settings/tokens
2. Используйте токен вместо пароля

**Вариант 2: GitHub CLI**
```bash
gh auth login
```

**Вариант 3: SSH ключ**
Настройте SSH ключ для GitHub (более сложно)

---

## 📋 Полная последовательность команд:

```bash
# 1. Перейти в папку проекта (если еще не там)
cd /Users/yury/Development/Personal/Books/Translate

# 2. Проверить статус
git status

# 3. Загрузить код (после создания репозитория на GitHub)
git push -u origin main
```

---

## 🎯 Итог:

**Где выполнять:** В терминале Cursor (или системном терминале)  
**В какой папке:** `/Users/yury/Development/Personal/Books/Translate`  
**Когда:** После создания репозитория на GitHub  
**Команда:** `git push -u origin main`

---

**Готово!** После выполнения команды ваш код будет на GitHub. 🚀



