# ⚡ Быстрая настройка GitHub аутентификации в Cursor

## ⚠️ Важно: Cursor не имеет раздела "Secrets"

Cursor IDE не имеет встроенного раздела для секретов. Используем системные методы.

---

## 🎯 Рекомендуемый способ: GitHub CLI (самый простой)

### Шаг 1: Установите GitHub CLI

**Mac:**
```bash
brew install gh
```

**Windows:**
- Скачайте: https://cli.github.com
- Установите установщик

**Linux:**
```bash
# Ubuntu/Debian
sudo apt install gh

# Fedora
sudo dnf install gh
```

### Шаг 2: Авторизуйтесь

Откройте терминал в Cursor (`Cmd + ~`) и выполните:

```bash
gh auth login
```

**Выберите:**
1. **GitHub.com** → Enter
2. **HTTPS** → Enter
3. **Authenticate Git with your GitHub credentials?** → `Y` (Yes)
4. **How would you like to authenticate?**
   - **Login with a web browser** (рекомендуется) - откроется браузер
   - **Paste an authentication token** - если есть PAT

**Если выбрали "Login with a web browser":**
- Откроется браузер
- Нажмите "Authorize GitHub CLI"
- Готово! ✅

### Шаг 3: Проверьте

```bash
gh auth status
```

Должно показать:
```
✓ Logged in to github.com as yugo-private
```

Теперь `git push` не будет запрашивать пароль! 🎉

---

## 🔄 Альтернатива: SSH ключи (без PAT)

Если не хотите устанавливать GitHub CLI:

### Шаг 1: Создайте SSH ключ

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

Нажмите Enter для всех вопросов.

### Шаг 2: Добавьте ключ в GitHub

1. **Скопируйте ключ:**
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```

2. **Добавьте в GitHub:**
   - Откройте: https://github.com/settings/keys
   - Нажмите "New SSH key"
   - Вставьте ключ
   - Сохраните

### Шаг 3: Измените remote на SSH

```bash
cd /Users/yury/Development/Personal/Books/Translate
git remote set-url origin git@github.com:yugo-private/book-translator.git
```

### Шаг 4: Проверьте

```bash
git push
```

Не должно запрашивать пароль! ✅

---

## 📚 Подробная инструкция

Полная инструкция в файле: `CURSOR_SECRETS_PAT.md`

---

## ✅ Что выбрать?

- **GitHub CLI** - проще всего, автоматическая настройка
- **SSH ключи** - не требует установки дополнительных программ

Оба способа работают одинаково хорошо! 🚀

