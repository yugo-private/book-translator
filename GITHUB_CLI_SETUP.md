# 🚀 Настройка GitHub CLI - Пошаговая инструкция

## ✅ Шаг 1: GitHub CLI установлен!

GitHub CLI успешно установлен на ваш компьютер.

---

## 🔐 Шаг 2: Авторизация

Теперь нужно авторизоваться. Выполните в терминале Cursor:

```bash
gh auth login
```

---

## 📋 Что выбрать в интерактивном меню:

### 1. **What account do you want to log into?**
   → Выберите: **GitHub.com** (нажмите Enter)

### 2. **What is your preferred protocol for Git operations?**
   → Выберите: **HTTPS** (нажмите Enter)

### 3. **Authenticate Git with your GitHub credentials?**
   → Введите: **Y** (Yes) и нажмите Enter

### 4. **How would you like to authenticate GitHub CLI?**
   → Выберите один из вариантов:

   **Вариант A: Login with a web browser** (рекомендуется)
   - Нажмите Enter
   - Откроется браузер
   - Нажмите "Authorize GitHub CLI"
   - Вернитесь в терминал - готово! ✅

   **Вариант B: Paste an authentication token**
   - Если у вас уже есть GitHub PAT
   - Вставьте токен (начинается с `ghp_...`)
   - Нажмите Enter

---

## ✅ Шаг 3: Проверка

После авторизации проверьте:

```bash
gh auth status
```

Должно показать:
```
✓ Logged in to github.com as yugo-private
```

---

## 🎯 Шаг 4: Проверка Git

Теперь `git push` не должен запрашивать пароль:

```bash
cd /Users/yury/Development/Personal/Books/Translate
git push
```

Если все настроено правильно, код загрузится без запроса пароля! 🎉

---

## 🆘 Если что-то пошло не так:

### Проблема: Не открывается браузер

**Решение:**
- Используйте вариант "Paste an authentication token"
- Создайте PAT: https://github.com/settings/tokens
- Вставьте токен в терминал

### Проблема: Git все еще запрашивает пароль

**Решение:**
1. Проверьте авторизацию:
   ```bash
   gh auth status
   ```

2. Убедитесь, что используете HTTPS:
   ```bash
   git remote -v
   # Должно быть: https://github.com/...
   ```

3. Очистите кэш Git:
   ```bash
   git credential-osxkeychain erase
   host=github.com
   protocol=https
   ```
   (Нажмите Enter дважды)

4. Попробуйте снова:
   ```bash
   git push
   ```

---

## 📚 Дополнительная информация:

- **GitHub CLI документация:** https://cli.github.com/manual/
- **Создание PAT:** https://github.com/settings/tokens

---

**Готово!** После выполнения этих шагов GitHub CLI будет настроен и Git не будет запрашивать пароль. 🚀

