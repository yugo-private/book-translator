# 🔐 Как настроить GitHub PAT для работы с Git в Cursor

## ⚠️ Важно: Cursor не имеет встроенного раздела "Secrets"

Cursor IDE не имеет встроенного раздела для хранения секретов. Вместо этого мы используем системные методы аутентификации Git.

---

## 📋 Что такое PAT?

**Personal Access Token (PAT)** - это токен для аутентификации с GitHub вместо пароля. Он нужен для безопасной работы с репозиториями из Cursor.

---

## 🎯 Рекомендуемый способ: GitHub CLI (самый простой)

### Шаг 1: Установите GitHub CLI (если еще не установлен)

#### 1.1. Откройте настройки GitHub

Перейдите по ссылке:
```
https://github.com/settings/tokens
```

Или вручную:
1. Откройте GitHub: https://github.com
2. Нажмите на ваш аватар (правый верхний угол)
3. Выберите **"Settings"**
4. В левом меню найдите **"Developer settings"**
5. Выберите **"Personal access tokens"** → **"Tokens (classic)"**

#### 1.2. Создайте новый токен

1. Нажмите **"Generate new token"** → **"Generate new token (classic)"**

2. Заполните форму:
   - **Note** (название): `Cursor IDE Access`
   - **Expiration** (срок действия):
     - `90 days` (рекомендуется для начала)
     - `No expiration` (если хотите, чтобы не истекал)
   - **Select scopes** (разрешения):
     - ✅ **`repo`** - Полный доступ к репозиториям (обязательно!)
     - ✅ **`workflow`** - Доступ к GitHub Actions (опционально)
     - ✅ **`read:org`** - Чтение организаций (опционально)

3. Нажмите **"Generate token"** (внизу страницы)

#### 1.3. Скопируйте токен

⚠️ **ВАЖНО:** Токен показывается только один раз!

1. Скопируйте токен (он начинается с `ghp_...`)
2. Сохраните его в безопасном месте (например, в менеджере паролей)

**Пример токена:**
```
ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

### Шаг 2: Авторизуйтесь через GitHub CLI

Откройте терминал в Cursor (`Cmd + ~` или `Ctrl + ~`) и выполните:

```bash
gh auth login
```

**Интерактивная настройка:**

1. **GitHub.com** → нажмите Enter
2. **HTTPS** → нажмите Enter
3. **Authenticate Git with your GitHub credentials?** → введите `Y` (Yes)
4. **How would you like to authenticate?** → выберите:
   - **Login with a web browser** (рекомендуется) - откроется браузер для авторизации
   - **Paste an authentication token** - если хотите вставить PAT вручную

**Если выбрали "Login with a web browser":**
- Откроется браузер
- Нажмите "Authorize GitHub CLI"
- Вернитесь в терминал - авторизация завершена

**Если выбрали "Paste an authentication token":**
- Вставьте ваш PAT (начинается с `ghp_...`)
- Нажмите Enter

### Шаг 3: Проверьте авторизацию

```bash
gh auth status
```

Должно показать:
```
✓ Logged in to github.com as yugo-private
```

---

## 🔄 Альтернативный способ: Настройка Git Credential Helper (Mac)

Если не хотите использовать GitHub CLI, можно настроить Git напрямую:

### Шаг 1: Создайте GitHub PAT

См. раздел "Создайте GitHub PAT" ниже.

### Шаг 2: Настройте Git для использования PAT

1. **Очистите старые учетные данные:**
   ```bash
   git credential-osxkeychain erase
   host=github.com
   protocol=https
   ```
   (Нажмите Enter дважды)

2. **При следующем `git push` Git запросит:**
   - **Username**: ваш GitHub username (`yugo-private`)
   - **Password**: вставьте ваш PAT (не пароль!)

3. **Git сохранит PAT в keychain** и больше не будет запрашивать

---

## 📝 Создайте GitHub PAT (если используете альтернативный способ)

---

### Шаг 3: Проверьте, что PAT добавлен

#### Проверка через терминал Cursor:

1. Откройте терминал в Cursor (`Cmd + ~` или `Ctrl + ~`)

2. Проверьте переменную окружения:
   ```bash
   echo $GITHUB_PAT
   ```
   
   Должен показать ваш токен (или пусто, если используется другой способ)

3. Попробуйте выполнить git операцию:
   ```bash
   git push
   ```
   
   Если PAT настроен правильно, запрос пароля не появится.

---

## 🔑 Альтернативный способ: SSH ключи (без PAT)

Если не хотите использовать PAT, можно настроить SSH:

### Шаг 1: Создайте SSH ключ

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

Нажмите Enter для всех вопросов (или укажите пароль для ключа).

### Шаг 2: Добавьте ключ в GitHub

1. **Скопируйте публичный ключ:**
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```

2. **Добавьте в GitHub:**
   - Откройте: https://github.com/settings/keys
   - Нажмите "New SSH key"
   - **Title**: `Cursor Mac` (или любое название)
   - **Key**: вставьте скопированный ключ
   - Нажмите "Add SSH key"

### Шаг 3: Измените remote URL на SSH

```bash
cd /Users/yury/Development/Personal/Books/Translate
git remote set-url origin git@github.com:yugo-private/book-translator.git
```

### Шаг 4: Проверьте

```bash
git push
```

Не должно запрашивать пароль или токен! ✅

---

## ✅ Проверка успешной настройки:

### Тест 1: Git операции без запроса пароля

```bash
cd /Users/yury/Development/Personal/Books/Translate
git push
```

Если не запрашивает пароль → PAT настроен правильно! ✅

### Тест 2: Проверка доступа к GitHub

```bash
# Если установлен GitHub CLI
gh auth status

# Должно показать:
# ✓ Logged in to github.com as yugo-private
```

---

## 🔒 Безопасность:

### ✅ Рекомендации:

1. **Не делитесь PAT** - это как пароль
2. **Используйте минимальные разрешения** - только `repo` если достаточно
3. **Установите срок действия** - не используйте `No expiration` без необходимости
4. **Храните в менеджере паролей** - на случай, если понадобится восстановить
5. **Отзывайте старые токены** - если больше не используете

### ❌ Что НЕ делать:

- ❌ Не коммитьте PAT в Git (он защищен `.gitignore`)
- ❌ Не отправляйте PAT в сообщениях или email
- ❌ Не используйте один PAT для всех проектов (создавайте отдельные)

---

## 🆘 Решение проблем:

### Проблема 1: GitHub CLI не установлен

**Решение для Mac:**
```bash
brew install gh
```

**Решение для Windows:**
- Скачайте установщик: https://cli.github.com
- Или используйте альтернативный способ (Git Credential Helper или SSH)

**Решение для Linux:**
```bash
# Ubuntu/Debian
sudo apt install gh

# Fedora
sudo dnf install gh

# Или скачайте с https://cli.github.com
```

### Проблема 2: Git все еще запрашивает пароль

**Решение:**
1. **Проверьте remote URL:**
   ```bash
   git remote -v
   # Должно быть: https://github.com/...
   ```

2. **Очистите кэш Git (Mac):**
   ```bash
   git credential-osxkeychain erase
   host=github.com
   protocol=https
   ```
   (Нажмите Enter дважды)

3. **Используйте GitHub CLI:**
   ```bash
   gh auth login
   ```

4. **Или используйте SSH** (см. раздел "SSH ключи" выше)

### Проблема 3: Токен истек

**Решение:**
1. Создайте новый токен (см. Шаг 1)
2. Обновите секрет в Cursor (см. Шаг 2)
3. Удалите старый токен на GitHub

---

## 📚 Дополнительные ресурсы:

- **GitHub PAT документация:** https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token
- **Cursor документация:** https://cursor.sh/docs
- **GitHub CLI:** https://cli.github.com

---

## 🎯 Краткая шпаргалка:

### Способ 1: GitHub CLI (рекомендуется)

```bash
# 1. Установите GitHub CLI
brew install gh  # Mac

# 2. Авторизуйтесь
gh auth login
# → GitHub.com
# → HTTPS
# → Authenticate Git? Yes
# → Login with web browser (или вставьте PAT)

# 3. Проверьте
gh auth status
git push
# Не должно запрашивать пароль ✅
```

### Способ 2: SSH ключи

```bash
# 1. Создайте SSH ключ
ssh-keygen -t ed25519 -C "your_email@example.com"

# 2. Добавьте в GitHub
cat ~/.ssh/id_ed25519.pub
# Скопируйте и добавьте на https://github.com/settings/keys

# 3. Измените remote на SSH
git remote set-url origin git@github.com:yugo-private/book-translator.git

# 4. Проверьте
git push
# Не должно запрашивать пароль ✅
```

---

**Готово!** После выполнения этих шагов Cursor сможет работать с GitHub без запроса пароля. 🚀

