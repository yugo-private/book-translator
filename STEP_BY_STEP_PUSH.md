# 📤 Пошаговая инструкция: Загрузка кода в GitHub

## Где выполнять команду `git push -u origin main`?

**Ответ:** В терминале Cursor (или системном терминале), в папке проекта.

---

## 🎯 Пошаговая инструкция:

### Шаг 1: Откройте терминал в Cursor

**Способ 1 (быстрый):**
- Нажмите `Ctrl + ~` (Windows/Linux) или `Cmd + ~` (Mac)
- Терминал откроется внизу экрана

**Способ 2 (через меню):**
- Меню → **Terminal** → **New Terminal**

**Способ 3 (системный терминал):**
- Откройте Terminal.app (Mac) или PowerShell (Windows)
- Перейдите в папку проекта командой:
  ```bash
  cd /Users/yury/Development/Personal/Books/Translate
  ```

---

### Шаг 2: Проверьте, что вы в правильной папке

В терминале выполните:

```bash
pwd
```

Должно показать:
```
/Users/yury/Development/Personal/Books/Translate
```

Если нет - выполните:
```bash
cd /Users/yury/Development/Personal/Books/Translate
```

---

### Шаг 3: Проверьте статус Git

```bash
git status
```

Вы должны увидеть что-то вроде:
```
On branch main
Your branch is ahead of 'origin/main' by X commits.
  (use "git push" to publish your local commits)
```

---

### Шаг 4: Создайте репозиторий на GitHub (если еще не создали)

1. Откройте в браузере: **https://github.com/new**

2. Заполните форму:
   - **Repository name**: `book-translator`
   - **Description**: "AI-powered Russian to English book translator"
   - **Private** (рекомендуется) или **Public**
   - ⚠️ **ВАЖНО:** НЕ добавляйте README, .gitignore, license (они уже есть в проекте!)

3. Нажмите **"Create repository"**

---

### Шаг 5: Загрузите код в GitHub

В терминале (который открыт в папке проекта) выполните:

```bash
git push -u origin main
```

**Что произойдет:**
- Git загрузит все файлы на GitHub
- Установится связь между локальной и удаленной веткой
- После этого можно будет просто писать `git push`

---

### Шаг 6: Авторизация (если потребуется)

GitHub может запросить логин и пароль:

**Вариант 1: Personal Access Token (рекомендуется)**
1. Создайте токен: https://github.com/settings/tokens
2. Нажмите "Generate new token (classic)"
3. Выберите scope: `repo`
4. Скопируйте токен
5. При запросе пароля вставьте токен

**Вариант 2: GitHub CLI**
```bash
gh auth login
```

---

## ✅ Проверка успешной загрузки

После выполнения `git push -u origin main` вы увидите:

```
Enumerating objects: 35, done.
Counting objects: 100% (35/35), done.
...
To https://github.com/yugo-private/book-translator.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

**Проверьте в браузере:**
Откройте: https://github.com/yugo-private/book-translator

Вы должны увидеть все файлы проекта!

---

## 📋 Краткая шпаргалка:

```bash
# 1. Откройте терминал в Cursor (Ctrl+~ или Cmd+~)

# 2. Проверьте папку
pwd
# Должно быть: /Users/yury/Development/Personal/Books/Translate

# 3. Загрузите код (после создания репозитория на GitHub)
git push -u origin main
```

---

## 🎯 Итог:

- **Где:** Терминал Cursor (или системный терминал)
- **В какой папке:** `/Users/yury/Development/Personal/Books/Translate`
- **Когда:** После создания репозитория на GitHub
- **Команда:** `git push -u origin main`

---

**Готово!** После выполнения команды ваш код будет на GitHub. 🚀



