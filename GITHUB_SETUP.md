# 🚀 Настройка GitHub репозитория

## Вопросы перед настройкой:

1. **Есть ли у вас аккаунт GitHub?**
   - Если нет → создайте на https://github.com/signup

2. **Какой тип репозитория предпочитаете?**
   - **Public** (публичный) - любой может видеть код
   - **Private** (приватный) - только вы и приглашенные

3. **Какое имя репозитория?**
   - Например: `book-translator`, `russian-to-english-translator`, `pintek-translator`
   - Или оставить текущее имя папки: `Translate`

4. **Нужна ли лицензия?**
   - MIT License (популярная, разрешает использование)
   - Или без лицензии (только для личного использования)

---

## Инструкция по настройке:

### Шаг 1: Инициализация Git репозитория

```bash
cd /Users/yury/Development/Personal/Books/Translate
git init
```

### Шаг 2: Добавление файлов

```bash
git add .
git commit -m "Initial commit: Book translation system"
```

### Шаг 3: Создание репозитория на GitHub

1. Зайдите на https://github.com/new
2. Заполните:
   - **Repository name**: `book-translator` (или другое имя)
   - **Description**: "AI-powered Russian to English book translator using MT + LLM post-editing"
   - **Public/Private**: выберите нужное
   - **НЕ добавляйте** README, .gitignore, license (они уже есть)
3. Нажмите "Create repository"

### Шаг 4: Подключение локального репозитория к GitHub

```bash
git remote add origin https://github.com/YOUR_USERNAME/book-translator.git
git branch -M main
git push -u origin main
```

**Замените `YOUR_USERNAME` на ваш GitHub username!**

---

## Для работы с ChatGPT:

### Способ 1: Прямая ссылка на файл

ChatGPT может читать файлы из GitHub по прямой ссылке:
```
https://github.com/YOUR_USERNAME/book-translator/blob/main/translator.py
```

### Способ 2: Клонирование репозитория

ChatGPT может попросить вас скопировать код, или вы можете дать ссылку на репозиторий.

### Способ 3: Использование GitHub Copilot

Если у вас есть GitHub Copilot, ChatGPT может работать с кодом через него.

---

## Что будет в репозитории:

✅ **Включено:**
- Весь исходный код Python
- Документация (README, ALGORITHM.md и т.д.)
- Конфигурационные файлы (requirements.txt, env_template.txt)
- Глоссарий (glossary.json, pintek_glossary.json)

❌ **Исключено (через .gitignore):**
- API ключи (.env)
- Виртуальное окружение (venv/)
- Переведенные файлы (Output/)
- Translation Memory (translation_memory.json)
- Временные файлы и логи

---

## Безопасность:

⚠️ **ВАЖНО:** 
- `.env` файл НЕ будет загружен в GitHub (защищен .gitignore)
- API ключи останутся только на вашем компьютере
- Если случайно закоммитили `.env` → смените все API ключи!

---

## Обновление репозитория:

После изменений в коде:

```bash
git add .
git commit -m "Описание изменений"
git push
```

---

## Клонирование на другой компьютер:

```bash
git clone https://github.com/YOUR_USERNAME/book-translator.git
cd book-translator
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp env_template.txt .env
# Отредактируйте .env и добавьте API ключи
```

---

Готово! После выполнения этих шагов ваш проект будет на GitHub и доступен для ChatGPT.

