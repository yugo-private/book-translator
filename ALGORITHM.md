# 🔄 Текущий алгоритм работы системы перевода

## Общая схема процесса

```
Исходный .docx файл (RU)
    ↓
[Чтение документа]
    ↓
[Разбиение на батчи (3-5 параграфов)]
    ↓
Для каждого батча:
    ↓
[ШАГ 0: Проверка Translation Memory]
    ├─ Найдено ≥98% → Возврат готового перевода
    └─ Не найдено → Продолжение
    ↓
[ШАГ 1: Машинный перевод через DeepL]
    ↓
[ШАГ 2: Применение глоссария]
    ↓
[ШАГ 3: LLM пост-редактирование]
    ↓
[ШАГ 4: Сохранение в Translation Memory]
    ↓
[Сборка всех батчей]
    ↓
[Сохранение в .docx файл]
    ↓
Финальный переведенный файл (EN)
```

---

## 📋 Детальное описание алгоритма

### Этап 0: Подготовка

**Файл:** `main.py` → `translator.py`

1. **Инициализация компонентов:**
   - Загрузка MT движка (DeepL/Google/Yandex)
   - Загрузка LLM редактора (GPT-4/Claude/DeepSeek/Grok/Crok)
   - Загрузка глоссария из `glossary.json`
   - Инициализация Translation Memory (если `--use-tm`)

2. **Чтение исходного файла:**
   - Извлечение всех параграфов из .docx
   - Подсчет количества параграфов

---

### Этап 1: Батчинг (разбиение на части)

**Файл:** `translator.py` → `translate_docx()`

**Процесс:**
- Параграфы объединяются в батчи по `batch_size` (по умолчанию 3-5)
- Каждый батч обрабатывается отдельно
- Это оптимизирует работу с API и улучшает качество

**Пример:**
```
26 параграфов, batch_size=3
→ 9 батчей: [1-3], [4-6], [7-9], ..., [25-26]
```

---

### Этап 2: Перевод каждого батча

**Файл:** `translator.py` → `translate_text()`

Для каждого батча выполняется полный цикл:

#### ШАГ 0: Проверка Translation Memory

**Файл:** `translation_memory.py` → `search()`

**Алгоритм:**
1. Берется текст батча
2. Поиск в базе Translation Memory похожих переводов
3. Используется алгоритм SequenceMatcher для сравнения строк
4. Если найдено совпадение ≥98% → возвращается готовый перевод
5. Если не найдено → переход к Шагу 1

**Преимущества:**
- Экономия API вызовов
- Консистентность перевода
- Быстрота для повторяющихся фраз

**Пример:**
```
Вход: "Артём и Катя были неразлучны"
TM поиск: находит 100% совпадение
Результат: возвращает "Tommy and Katie were inseparable"
Время: ~0.01 сек
```

---

#### ШАГ 1: Машинный перевод (DeepL)

**Файл:** `mt_engines.py` → `DeepLEngine.translate()`

**Алгоритм:**
1. Текст отправляется в DeepL API
2. Параметры:
   - `source_lang="RU"`
   - `target_lang="EN-US"` (американский английский)
3. DeepL выполняет перевод
4. Возвращается базовый английский текст

**Что получаем:**
- Грамотный перевод с правильной грамматикой
- Может быть буквальным для художественного текста
- Хорошая база для дальнейшего улучшения

**Пример:**
```
Вход: "Артём и Катя были неразлучны: одноклассники, друзья и настоящие соратники"
DeepL: "Artyom and Katya were inseparable: classmates, friends and true companions"
Время: ~1-2 сек
```

---

#### ШАГ 2: Применение глоссария

**Файл:** `glossary.py` → `apply()`

**Алгоритм:**
1. Загружается глоссарий из `glossary.json`
2. Записи сортируются по длине (от длинных к коротким)
   - Важно! Чтобы "Артём" заменялся раньше, чем "Арт"
3. Для каждой записи:
   - Поиск исходного термина в тексте (регистронезависимый)
   - Замена на перевод из глоссария
4. Возвращается текст с примененным глоссарием

**Алгоритм замены:**
- Использует регулярные выражения (regex)
- Игнорирует регистр (case-insensitive)
- Заменяет все вхождения

**Пример:**
```
Вход (после DeepL): "Artyom and Katya were inseparable"
Глоссарий: {"Артём": "Tommy", "Катя": "Katie"}
Поиск и замена:
  "Artyom" → "Tommy"
  "Katya" → "Katie"
Результат: "Tommy and Katie were inseparable"
Время: ~0.01 сек
```

**Важно:** Глоссарий применяется к переводу DeepL, а не к оригиналу!

---

#### ШАГ 3: LLM пост-редактирование

**Файл:** `llm_post_editor.py` → `post_edit()`

**Алгоритм:**

##### 3.1. Создание промпта

**Файл:** `llm_post_editor.py` → `_create_prompt()`

Система создает промпт, который включает:

1. **Роль:** "You are a professional literary translator specializing in Russian to American English translation."

2. **Инструкции:**
   - Сохранить смысл и тон оригинала
   - Сделать перевод естественным на американском английском
   - Сохранить эмоциональную окраску
   - Исправить буквальные переводы и грамматические ошибки
   - Использовать термины из глоссария
   - Сохранить стиль диалогов и голос персонажей
   - Сохранить повествовательный поток и темп

3. **Оригинальный русский текст** (для контекста)

4. **Переведенный текст** (после DeepL + глоссарий)

5. **Глоссарий** (если используется):
   ```
   Глоссарий для консистентности:
   - Артём: Tommy
   - Катя: Katie
   - Пинтек: Pintek
   ...
   ```

##### 3.2. Отправка в LLM

**Параметры запроса:**
- `model`: зависит от LLM
  - GPT-4: `"gpt-4o"`
  - Claude: `"claude-3-5-sonnet-20241022"`
  - DeepSeek: `"deepseek-chat"`
  - Grok: `"grok-beta"`
- `temperature`: 0.7 (баланс креативности и точности)
- `max_tokens`: 4000 (максимальная длина ответа)

##### 3.3. Получение улучшенного перевода

LLM анализирует:
- Оригинальный текст (для понимания контекста и стиля)
- Базовый перевод (что нужно улучшить)
- Глоссарий (какие термины использовать)

И возвращает улучшенный, естественный перевод.

**Пример:**
```
Вход (после глоссария): "Tommy and Katie were inseparable: classmates, friends and true companions"
LLM анализ:
  - "true companions" звучит формально
  - Можно сделать более естественным для детской книги
  - Добавить эмоциональную окраску
Результат: "Tommy and Katie were inseparable: classmates, friends, and true partners in all their adventures"
Время: ~3-10 сек
```

---

#### ШАГ 4: Сохранение в Translation Memory

**Файл:** `translation_memory.py` → `add()`

**Алгоритм:**
1. Берется оригинальный текст и финальный перевод
2. Создается запись:
   ```json
   {
     "source": "Артём и Катя были неразлучны...",
     "target": "Tommy and Katie were inseparable...",
     "metadata": {}
   }
   ```
3. Проверка на дубликаты
4. Сохранение в `translation_memory.json`

**Преимущества:**
- При повторении той же фразы → мгновенный перевод
- Консистентность между главами
- Экономия на API вызовах

---

### Этап 3: Сборка результатов

**Файл:** `translator.py` → `translate_docx()`

**Алгоритм:**
1. Все переведенные батчи собираются вместе
2. Разделение обратно на параграфы (по `\n\n`)
3. Если количество не совпадает → весь батч сохраняется как один параграф
4. Формируется список переведенных параграфов

---

### Этап 4: Сохранение результата

**Файл:** `docx_handler.py` → `write_docx()`

**Алгоритм:**
1. Создается новый документ Word
2. Если указан исходный файл → копируется форматирование
3. Все переведенные параграфы записываются в документ
4. Сохранение в указанный файл

**Важно:** Сохраняется только переведенный текст, без оригинала!

---

## 🔄 Полный цикл на примере

### Пример перевода одного батча:

**Исходный текст:**
```
Артём и Катя были неразлучны: одноклассники, друзья и настоящие соратники во всех своих приключениях.
Дети жили в городе, где летом всё плавилось от жары и скуки.
Учебный год подходил к концу, и они мечтали провести июнь вместе у бабушки Кати в деревне.
```

**ШАГ 0: Translation Memory**
```
Поиск: "Артём и Катя были неразлучны..."
Результат: не найдено (первый раз переводим)
Время: ~0.01 сек
```

**ШАГ 1: DeepL перевод**
```
Вход: "Артём и Катя были неразлучны..."
DeepL: "Artyom and Katya were inseparable: classmates, friends and true companions in all their adventures.
The children lived in a city where everything melted in the summer from heat and boredom.
The school year was coming to an end, and they dreamed of spending June together with Katya's grandmother in the village."
Время: ~1-2 сек
```

**ШАГ 2: Применение глоссария**
```
Глоссарий: {"Артём": "Tommy", "Катя": "Katie"}
Поиск и замена:
  "Artyom" → "Tommy"
  "Katya" → "Katie" (2 раза)
Результат: "Tommy and Katie were inseparable: classmates, friends and true companions in all their adventures.
The children lived in a city where everything melted in the summer from heat and boredom.
The school year was coming to an end, and they dreamed of spending June together with Katie's grandmother in the village."
Время: ~0.01 сек
```

**ШАГ 3: LLM пост-редактирование**
```
Промпт для GPT-4:
  "You are a professional literary translator...
   Original: Артём и Катя были неразлучны...
   Translated: Tommy and Katie were inseparable...
   Glossary: Артём: Tommy, Катя: Katie"
   
GPT-4 анализ:
  - "true companions" → "true partners in adventure" (более естественно)
  - "everything melted" → "everything seemed to melt" (более образно)
  - Улучшение стиля для детской книги
  
Результат: "Tommy and Katie were inseparable: classmates, friends, and true partners in all their adventures.
The children lived in a city where everything seemed to melt in the summer heat and boredom.
The school year was coming to an end, and they dreamed of spending June together at Katie's grandmother's house in the village."
Время: ~3-10 сек
```

**ШАГ 4: Сохранение в TM**
```
Сохранение:
  Source: "Артём и Катя были неразлучны..."
  Target: "Tommy and Katie were inseparable..."
Время: ~0.01 сек
```

**Общее время:** ~5-15 секунд на батч

---

## ⚙️ Параметры и настройки

### Параметры, влияющие на процесс:

1. **batch_size** (размер батча)
   - По умолчанию: 3-5 параграфов
   - Больше → лучше контекст, но дороже
   - Меньше → дешевле, но может потерять контекст

2. **temperature** (для LLM)
   - По умолчанию: 0.7
   - Выше → более креативный перевод
   - Ниже → более точный перевод

3. **max_tokens** (для LLM)
   - По умолчанию: 4000
   - Ограничивает длину ответа LLM

4. **similarity_threshold** (для TM)
   - По умолчанию: 0.98 (98%)
   - Минимальная схожесть для использования из TM

---

## 📊 Временная сложность

**Для одного батча (3-5 параграфов, ~300-500 слов):**
- Шаг 0 (TM): ~0.01 сек
- Шаг 1 (DeepL): ~1-2 сек
- Шаг 2 (Глоссарий): ~0.01 сек
- Шаг 3 (LLM): ~3-10 сек
- Шаг 4 (TM сохранение): ~0.01 сек

**Итого:** ~5-15 секунд на батч

**Для одной главы (26 параграфов, batch_size=3):**
- ~9 батчей × ~10 сек = **~90 секунд = 1.5 минуты**

**Для всей книги (26 страниц, ~200 параграфов):**
- ~40-70 батчей × ~10 сек = **~7-12 минут**

---

## 🎯 Ключевые особенности алгоритма

1. **Двухэтапный процесс:**
   - MT (DeepL) → быстрый и точный базовый перевод
   - LLM → улучшение естественности и стиля

2. **Глоссарий применяется между этапами:**
   - После MT перевода
   - Перед LLM пост-редактированием
   - LLM получает правильные термины для улучшения

3. **Translation Memory:**
   - Проверяется в начале каждого батча
   - Сохраняется в конце каждого батча
   - Обеспечивает консистентность и экономию

4. **Батчинг:**
   - Оптимизирует работу с API
   - Улучшает качество (больше контекста)
   - Балансирует стоимость и качество

---

## ✅ Итоговая схема алгоритма

```
┌─────────────────────────────────────────┐
│   Исходный .docx файл (RU)              │
└──────────────┬──────────────────────────┘
               │
               ▼
    ┌──────────────────────┐
    │ Чтение документа     │
    │ Извлечение параграфов│
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │ Разбиение на батчи   │
    │ (3-5 параграфов)      │
    └──────────┬───────────┘
               │
               ▼
    Для каждого батча:
    ┌──────────────────────┐
    │ Translation Memory? │ ──ДА──► Готовый перевод
    │   (если включена)    │
    └──────────┬───────────┘
               │ НЕТ
               ▼
    ┌──────────────────────┐
    │  DeepL Translation   │
    │   (Машинный перевод) │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │  Применение          │
    │  Глоссария           │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │  LLM Post-Edit       │
    │  (GPT-4/Claude/etc)  │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │  Сохранение в TM?    │ ──ДА──► Сохранить
    │   (если включена)    │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │  Переведенный батч   │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │  Сборка всех батчей  │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │  Сохранение в .docx  │
    │  (только перевод!)   │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │  Финальный файл (EN) │
    └──────────────────────┘
```

---

Это полное описание текущего алгоритма работы системы! 🎯

